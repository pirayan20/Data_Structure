#ifndef __STUDENT_H__
#define __STUDENT_H__

template <typename T>
void CP::stack<T>::moveInsert(int k,CP::stack<T> &s2, int m) {
  //your code here
  m = (m > s2.size()) ? m2.size() : m;
  k = (k > mSize()) ? mSize() : k;
  int idx = 0;
  T *arr = new T[mSize + m];
  int s = mSize - k - 1;
  for (int i = 0; i<m ; i++){

  }

}
#endif
